import { HandTypeNamePipe } from './hand-type-name.pipe';

describe('HandTypeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new HandTypeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
