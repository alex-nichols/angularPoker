import { LoginLinkDirective } from './login-link.directive';

describe('LoginLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginLinkDirective();
    expect(directive).toBeTruthy();
  });
});
